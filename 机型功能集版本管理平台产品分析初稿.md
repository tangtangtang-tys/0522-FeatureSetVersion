# 机型功能集版本管理平台产品分析初稿

## 1. 背景与分析目标

当前平台由研发人员基于自身对功能集版本管理的理解开发，截图中呈现为“功能集管理系统”，主要围绕功能项进行维护，已具备关键字查询、新增、批量导入功能集、批量导入功能集及版本、状态启停、编辑、删除、版本管理等能力。

从摄像机设备相关物联网产品视角看，该平台已经具备“功能项资产化”的雏形，但若要支撑机型功能集及功能集版本规划管理，还需要从研发工具升级为跨产品、研发、测试、供应链、销售、交付、运维协同的产品能力管理平台。

本初稿目标不是否定现有实现，而是从专业产品角度分析：

- 当前平台解决了什么问题。
- 当前平台容易遗漏哪些产品管理视角。
- 机型功能集版本管理应覆盖哪些对象、流程、角色和数据闭环。
- 后续应如何分阶段规划，避免一次性做大但无法落地。

## 2. 核心概念界定

### 2.1 功能项

功能项是最小可管理的能力定义，例如网络类型、镜头数量、低功耗唤醒支持、电机信息、连接类型、4G 基站上报支持等。

功能项不应只是一个字段或枚举，还应包含：

- 功能编码。
- 功能名称。
- 功能定义。
- 取值类型。
- 取值范围。
- 适用机型范围。
- 依赖条件。
- 互斥条件。
- 默认值。
- 是否可售卖。
- 是否可配置。
- 是否需要认证或合规约束。
- 是否影响固件、App、云平台、算法、测试用例。

### 2.2 功能集

功能集是面向某一类机型或某一产品线的能力集合。它不是单个功能项列表的简单叠加，而是对外可承诺、对内可交付的能力包。

在摄像机 IoT 场景中，一个功能集通常会同时涉及：

- 硬件能力：传感器、镜头、云台、电机、红外、补光、麦克风、扬声器、存储、网络模组。
- 固件能力：视频编码、码流、录像、告警、协议接入、OTA、日志。
- 智能能力：人形检测、车辆检测、区域入侵、越界检测、目标追踪、低功耗唤醒。
- 云平台能力：设备接入、消息推送、云存储、远程配置、健康诊断。
- App/Web/VMS 能力：页面入口、配置能力、预览回放、告警展示、设备管理。
- 商业能力：标准功能、增值功能、License 功能、项目定制功能。

### 2.3 功能集版本

功能集版本是某一功能集在特定时间点的能力快照。它回答的是“这个机型或产品线在这个版本中承诺支持什么”。

功能集版本应区别于软件版本：

- 功能集版本是产品定义语言，描述能力边界和承诺范围。
- 固件版本、App 版本、云平台版本是研发实现语言，描述能力由哪些系统实现。
- 同一个功能集版本可能需要多个软件版本共同支撑。
- 同一个软件版本也可能服务多个机型功能集。

### 2.4 机型功能集

机型功能集是功能集与具体机型、SKU、硬件平台、区域、渠道、项目之间的绑定关系。

它需要回答：

- 某机型支持哪些功能。
- 每个功能的具体取值是什么。
- 这些功能从哪个功能集版本开始支持。
- 哪些功能是标准支持，哪些是可选支持。
- 哪些功能仅在特定固件、算法包、App 或云平台版本后可用。
- 已出货设备升级后是否能获得新功能。

## 3. 当前平台能力观察

基于截图，当前平台至少具备以下能力：

- 支持按编码或名称搜索功能项。
- 支持新增功能项。
- 支持批量导入功能集。
- 支持批量导入功能集及版本。
- 支持功能项状态启用。
- 支持功能项编辑和删除。
- 支持进入单个功能项的版本管理。
- 表格中包含 ID、编码、名称、说明、状态、创建时间、操作等基础字段。

截图中的功能项包括：

- 网络类型。
- 基础云子码流支持。
- 枪机分屏支持。
- 镜头数量。
- 低功耗唤醒支持。
- 四摄远近焦标识。
- 新添加方式。
- 电机信息。
- 连接类型。
- 4G 基站上报支持。

这些功能项具有明显的摄像机设备属性，说明平台已经开始沉淀设备能力字典。

## 4. 当前平台的积极价值

### 4.1 建立了功能项字典意识

研发团队已经意识到功能能力需要结构化管理，而不是分散在代码、表格、文档、测试用例或人员经验中。这是功能集版本管理的第一步。

### 4.2 具备批量导入能力

摄像机产品线往往机型多、SKU 多、历史数据多。批量导入能力说明平台考虑了从旧表格或历史资料迁移数据的实际需要。

### 4.3 初步支持版本管理入口

单个功能项已有“版本管理”入口，说明研发团队并未只把功能项当静态字典，而是考虑到了功能随时间演进的问题。

### 4.4 状态字段有利于生命周期管理

启用状态能够支撑功能项上架、下架、废弃、冻结等后续能力，是功能治理的基础字段。

## 5. 需要辩证看待的问题

### 5.1 研发视角强，产品承诺视角不足

当前列表更接近“研发可识别的能力字段表”。这对系统实现很有帮助，但产品管理还需要知道这些字段如何形成可售卖、可交付、可验收的功能包。

例如“networkType”在研发看来是枚举字段，但在产品视角中，它会影响：

- 机型定位：WiFi 机、4G 机、有线机、双网机。
- BOM 成本：是否需要蜂窝模组、天线、SIM 卡。
- 区域合规：不同国家地区的无线认证要求。
- App 体验：添加方式、网络配置流程不同。
- 售后诊断：弱网、离线、流量消耗问题。

因此，平台不应只记录字段值，还应记录功能背后的产品含义和业务影响。

### 5.2 有功能项版本，但缺少功能集整体版本基线

功能项可以有版本，但产品交付时更关心的是某个机型在某个版本下的完整能力基线。

如果只管理单个功能项版本，容易出现：

- 单个功能项变更可追溯，但整机能力不可追溯。
- 某个机型到底支持哪些能力，需要人工拼装。
- 测试无法基于完整功能集版本生成验收范围。
- 销售无法拿到稳定的机型能力说明。

平台需要补足“功能集版本基线”的概念，即把一批功能项及其取值、状态、依赖、适用范围固化为一个可发布版本。

### 5.3 有导入和维护动作，但流程闭环不足

截图中的操作偏数据维护。真正的版本规划管理需要覆盖从需求提出到发布后反馈的完整链路。

建议闭环如下：

1. 需求来源：市场、客户项目、竞品、法规、研发平台演进、售后问题。
2. 功能立项：明确功能价值、适用机型、投入产出、优先级。
3. 影响评估：产品、硬件、固件、App、云平台、算法、测试、供应链共同评估。
4. 功能定义：沉淀功能项、取值、依赖、互斥、默认策略。
5. 功能集编排：绑定到机型、产品线、区域、渠道、项目。
6. 版本冻结：形成可评审的功能集版本基线。
7. 研发实现：关联固件、App、云平台、算法、配置后台等实现版本。
8. 测试验证：根据功能集版本生成测试范围和验收结论。
9. 发布生效：对销售、交付、客服、运营输出可用说明。
10. 运行反馈：收集缺陷、售后问题、客户需求和数据指标。
11. 变更归档：记录变更原因、影响范围、审批结论和历史版本。

### 5.4 缺少角色权限和协作机制

功能集版本管理不是单角色系统。不同角色关注点不同：

- 产品经理关注能力边界、版本规划、商业策略和客户承诺。
- 研发关注字段定义、实现方案、兼容性和技术依赖。
- 测试关注验收标准、测试覆盖和版本冻结范围。
- 项目交付关注客户定制、交付清单和现场升级风险。
- 销售/售前关注机型卖点、可售能力和区域差异。
- 售后/运维关注设备实际能力、问题定位和升级策略。
- 供应链关注硬件 BOM、替代料、模组差异和生产批次。

如果平台仅服务研发，会形成“研发知道字段，业务仍靠问人”的状态。平台应逐步成为跨角色的能力事实源。

### 5.5 删除能力风险较高

截图中功能项有“删除”操作。对于功能集基础字典而言，删除需要慎重。

功能项一旦被历史机型、历史版本、测试用例、客户项目或线上设备引用，物理删除会破坏追溯链路。

建议改为：

- 默认禁用物理删除。
- 支持废弃、停用、归档。
- 若确需删除，必须先校验引用关系。
- 删除动作应记录审计日志。
- 历史版本中仍应保留当时的功能定义快照。

### 5.6 功能说明偏自然语言，结构化程度不足

截图中“说明”字段将枚举含义写在一段文本里，例如“网络类型 1:仅wifi 2:仅4G 3:wifi+4G ...”。

这种方式初期灵活，但长期会带来问题：

- 机器难以解析。
- 前端表单难以自动生成。
- 测试用例难以自动匹配。
- 导入数据容易不一致。
- 枚举值变更难以判断影响范围。

建议将枚举值、单位、数据类型、约束条件结构化管理，而说明字段只保留业务解释。

### 5.7 缺少兼容性与依赖关系表达

摄像机功能之间存在大量依赖和互斥关系。例如：

- 低功耗唤醒依赖电池供电、PIR 或低功耗芯片能力。
- 4G 基站上报依赖蜂窝模组、SIM、基站定位能力和云端解析。
- 双码流能力与 SoC 编码能力、分辨率、码率和云端存储策略有关。
- 云台电机信息会影响 App 控制入口、预置位、巡航、追踪等功能。
- NVR MQTT、IPC MQTT、车载 TCP 等连接类型会影响设备接入链路。

如果平台不能表达依赖关系，后续功能集组合时会产生无效组合。

### 5.8 缺少发布、冻结和变更审批状态

当前只有功能项启用状态，但版本管理至少需要以下状态：

- 草稿：可编辑，尚未评审。
- 待评审：提交跨角色评审。
- 已冻结：进入研发或测试基线，不允许随意变更。
- 已发布：对外可引用，可进入销售和交付资料。
- 已废弃：不再用于新机型，但历史可追溯。
- 已下线：功能不再支持，需有替代方案和通知机制。

没有状态机，就难以形成版本纪律。

### 5.9 版本号、取值编码和能力承诺混用

从截图中的“说明”字段看，当前写法类似“网络类型 1:仅 wifi 2:仅 4G 3:wifi+4G”。如果这里的数字表示协议枚举值，那么它应该被定义为“取值编码”或“协议值”；如果这里的数字表示版本号，那么数字后面的内容应该进入“该版本支持的能力清单”。两者不能混在同一个说明字段中。

这类混用会带来几个严重问题：

- 用户无法判断数字代表版本、枚举值、排序，还是设备协议值。
- 产品无法表达“某个版本支持多个能力值”的关系。
- 研发无法稳定判断字段变更是定义变更、枚举扩展，还是版本升级。
- 测试无法基于版本号生成准确的测试范围。
- 销售和交付无法确认某机型在某版本下到底能承诺什么。

产品侧需要先统一语言：功能项描述“要管理什么能力”，取值描述“这个能力有哪些可能状态”，版本描述“某个时间点或发布节点承诺支持哪些能力”，机型绑定描述“某个具体机型实际启用了哪些能力”。

## 6. 目标定位建议

平台建议定位为：摄像机 IoT 产品的机型能力事实源和功能集版本治理平台。

它应服务三个核心目标：

### 6.1 对产品

帮助产品经理建立可规划、可评审、可发布、可追溯的机型能力体系。

### 6.2 对研发

帮助研发明确功能字段、实现边界、版本依赖和兼容性关系，减少口头沟通和重复确认。

### 6.3 对业务

帮助销售、售前、交付、客服快速确认某机型在某区域、某项目、某软件版本下到底支持什么，避免超卖、错卖和交付争议。

## 7. 核心用户与关键诉求

### 7.1 产品经理

核心诉求：

- 维护功能项和功能集定义。
- 规划不同机型的标准能力和差异化能力。
- 管理版本节奏和发布范围。
- 输出销售、测试、交付可理解的能力说明。

### 7.2 研发人员

核心诉求：

- 明确功能项编码、取值、依赖和影响系统。
- 关联固件、App、云平台、算法等实现版本。
- 识别功能变更影响范围。
- 避免同义字段、重复字段和历史字段失控。

### 7.3 测试人员

核心诉求：

- 根据功能集版本生成测试范围。
- 判断某机型功能是否需要回归。
- 记录功能项验证状态。
- 关联测试用例、测试报告和缺陷。

### 7.4 销售/售前

核心诉求：

- 查询机型能力清单。
- 对比不同机型功能差异。
- 确认客户需求是否标准支持。
- 获取可对外承诺的功能说明。

### 7.5 项目交付/售后

核心诉求：

- 查询项目版本与设备实际能力。
- 判断现场问题是能力不支持、版本未升级、配置未开启还是故障。
- 追溯历史版本能力变化。
- 获取升级和替代方案。

## 8. 建议的信息架构

平台建议由以下模块组成：

### 8.1 功能项字典

管理最小功能项。

关键字段建议包括：

- 功能编码。
- 功能名称。
- 功能分类。
- 功能描述。
- 数据类型。
- 取值方式。
- 枚举值。
- 默认值。
- 单位。
- 是否必填。
- 是否可售卖。
- 是否可配置。
- 是否影响硬件。
- 是否影响固件。
- 是否影响 App。
- 是否影响云平台。
- 是否影响算法。
- 依赖功能。
- 互斥功能。
- 适用产品线。
- 生命周期状态。
- 负责人。
- 创建时间。
- 更新时间。

### 8.2 功能集管理

管理一组功能项的集合。

建议支持：

- 创建功能集。
- 选择功能项。
- 设置功能项取值。
- 设置功能项是否必选、可选、隐藏、License 控制。
- 设置功能项适用条件。
- 复制已有功能集生成新功能集。
- 功能集差异对比。
- 功能集导入导出。

### 8.3 功能集版本管理

管理功能集的版本基线。

建议支持：

- 版本草稿。
- 版本说明。
- 变更内容。
- 影响范围。
- 版本状态。
- 评审记录。
- 冻结记录。
- 发布记录。
- 历史版本对比。
- 回滚参考。

### 8.4 机型功能集绑定

管理机型、SKU、硬件平台与功能集版本的关系。

建议支持：

- 机型编码。
- SKU 编码。
- 硬件平台。
- 硬件版本。
- BOM 版本。
- 区域。
- 渠道。
- 项目。
- 标准功能集版本。
- 项目定制功能集版本。
- 生效时间。
- 停用时间。

### 8.5 软件版本映射

管理功能集版本与实现版本之间的关系。

建议支持：

- 固件版本。
- App 版本。
- 云平台版本。
- Web/VMS 版本。
- 算法包版本。
- 配置后台版本。
- 最低支持版本。
- 推荐版本。
- 不兼容版本。

### 8.6 测试与验收

管理功能集版本的验证闭环。

建议支持：

- 功能项测试用例关联。
- 测试范围自动生成。
- 验证状态。
- 测试报告。
- 缺陷关联。
- 验收结论。
- 发布准入检查。

### 8.7 查询与对比

支持业务高频场景。

建议支持：

- 按机型查能力。
- 按功能查机型。
- 按项目查版本。
- 按软件版本查支持能力。
- 多机型能力对比。
- 多版本差异对比。
- 导出销售版能力清单。
- 导出测试版功能矩阵。

## 9. 建议的流程闭环

### 9.1 新功能引入流程

1. 需求提出：市场、客户项目、竞品、法规、售后或研发提出。
2. 初步归类：判断是新增功能项、已有功能项扩展取值，还是功能集组合变化。
3. 产品评估：明确用户价值、商业价值、适用机型、优先级。
4. 技术评估：评估硬件、固件、云、App、算法、测试影响。
5. 数据建模：创建或更新功能项，结构化定义取值和约束。
6. 功能集编排：加入目标功能集并设置适用条件。
7. 版本评审：形成版本草稿并提交评审。
8. 版本冻结：评审通过后冻结范围。
9. 实现验证：研发实现并完成测试验收。
10. 发布生效：功能集版本发布，对业务侧可见。
11. 复盘沉淀：记录实际问题、客户反馈和后续优化点。

### 9.2 功能变更流程

1. 变更申请。
2. 引用关系分析。
3. 影响范围分析。
4. 兼容性判断。
5. 评审审批。
6. 生成新版本。
7. 测试回归。
8. 发布通知。
9. 历史归档。

### 9.3 功能下线流程

1. 下线原因确认。
2. 影响机型和项目盘点。
3. 替代方案确认。
4. 客户和内部通知策略确认。
5. 设置废弃或下线状态。
6. 禁止新机型引用。
7. 保留历史版本追溯。
8. 监控线上反馈。

### 9.4 项目定制转标准流程

1. 项目提出定制能力。
2. 评估是否具有通用价值。
3. 若仅项目可用，进入项目定制功能集。
4. 若具备通用价值，进入标准功能集规划。
5. 标准化命名、编码、取值和依赖。
6. 纳入功能集版本评审。
7. 发布到目标产品线。

## 10. 数据模型建议

初步建议抽象以下核心实体：

- 功能项 Feature。
- 功能项取值 FeatureValue。
- 功能集 FeatureSet。
- 功能集版本 FeatureSetVersion。
- 功能集版本明细 FeatureSetVersionItem。
- 机型 Model。
- SKU。
- 硬件平台 HardwarePlatform。
- 软件版本 SoftwareVersion。
- 版本映射 VersionMapping。
- 项目 Project。
- 区域 Region。
- 渠道 Channel。
- 测试用例 TestCase。
- 测试结果 TestResult。
- 变更申请 ChangeRequest。
- 审批记录 ApprovalRecord。
- 审计日志 AuditLog。

关键关系建议：

- 一个功能集包含多个功能项。
- 一个功能集可以有多个版本。
- 一个功能集版本应冻结功能项及取值快照。
- 一个机型可以绑定一个标准功能集版本。
- 一个机型在特定项目中可以绑定项目定制功能集版本。
- 一个功能集版本可以映射多个软件实现版本。
- 一个功能项可以关联多个测试用例。
- 一个变更申请可以影响多个功能项、功能集版本和机型。

## 11. 产品侧字段定义与标准规范

当前平台最大的问题不是页面功能少，而是基础对象定义不清。产品侧应先统一字段标准，再让研发按标准建模和实现。否则后续即使增加审批、导入、对比、版本管理，也会建立在错误口径之上。

### 11.1 字段设计原则

字段设计应遵循以下原则：

- 用户可理解：业务用户看到字段名称后，应能判断字段含义，不需要理解研发协议。
- 机器可计算：枚举、布尔、数值、版本、状态等字段应结构化，不应藏在自然语言说明里。
- 历史可追溯：已发布版本中的字段定义、取值和能力承诺不能被覆盖修改。
- 责任可归属：关键字段应有维护人、审批人和变更原因。
- 场景可区分：产品定义、研发实现、设备协议、测试验收、销售承诺不能混用同一字段表达。
- 变更可评估：字段变更应能判断影响哪些机型、功能集版本、软件版本、测试用例和项目。

### 11.2 必须拆开的四类对象

建议将当前“名称 + 说明 + 版本管理”的混合模型拆成四类对象。

| 对象 | 产品定义 | 示例 | 主要用户 | 不应承担的内容 |
| --- | --- | --- | --- | --- |
| 功能项 | 描述一类可被管理的能力 | 网络连接类型、镜头数量、低功耗唤醒 | 产品、研发、测试 | 不直接表达某机型是否支持 |
| 功能取值 | 描述功能项的可选值或数据范围 | 仅 WiFi、仅 4G、WiFi+4G、有线+WiFi | 产品、研发、测试 | 不表达版本号 |
| 功能集版本 | 描述某个功能集在某次发布时承诺支持的能力组合 | IPC 标准功能集 V1.0 | 产品、测试、销售、交付 | 不替代固件版本 |
| 机型功能配置 | 描述某个机型实际绑定的功能集版本和取值 | A100 绑定 IPC 标准功能集 V1.0，网络连接类型=WiFi+4G | 产品、交付、售后 | 不修改功能项基础定义 |

### 11.3 功能项字段规范

功能项用于定义“这是什么能力”，不是定义“这个能力有哪些版本”。

建议字段如下：

| 字段 | 定义 | 示例 | 填写规则 |
| --- | --- | --- | --- |
| 功能项编码 | 功能项的唯一标识，供系统和接口引用 | `network_type` | 使用英文小写加下划线，创建后原则上不可修改 |
| 功能项名称 | 面向用户展示的标准中文名称 | 网络连接类型 | 使用业务可理解名称，避免研发缩写 |
| 功能分类 | 功能所属能力域 | 网络连接 | 从标准分类中选择 |
| 功能定义 | 说明该功能项管理的能力边界 | 描述设备支持的网络接入方式 | 只描述能力含义，不写枚举列表 |
| 数据类型 | 功能取值的数据结构 | 枚举、布尔、整数、字符串、多选枚举 | 从系统预置类型选择 |
| 是否多选 | 一个机型是否可同时选择多个取值 | 是/否 | 如设备可同时支持 WiFi 和 4G，则应支持多选或组合枚举 |
| 默认值 | 未特别配置时采用的标准取值 | 仅 WiFi | 必须来自功能取值表 |
| 适用范围 | 适用产品线或设备类型 | IPC、NVR、低功耗相机 | 不适用范围不得引用 |
| 依赖条件 | 启用该功能所需前置条件 | 4G 能力依赖蜂窝模组 | 关联其他功能项或硬件平台 |
| 互斥条件 | 不能同时存在的功能条件 | 低功耗电池机不支持常久在线预览 | 关联其他功能项或功能取值 |
| 业务可见性 | 是否对销售、售前、交付可见 | 内部可见/业务可见/对外可见 | 对外可见字段必须经过产品审核 |
| 生命周期状态 | 功能项当前状态 | 草稿、启用、废弃、停用 | 已被引用不可直接删除 |
| 维护负责人 | 功能项口径负责人 | 产品经理姓名或角色 | 必填 |

### 11.4 功能取值字段规范

功能取值用于定义“这个能力可以取什么值”。截图中写在“说明”里的 `1:仅wifi 2:仅4G` 应该拆到功能取值表。

建议字段如下：

| 字段 | 定义 | 示例 | 填写规则 |
| --- | --- | --- | --- |
| 取值编码 | 面向产品和业务的标准取值标识 | `wifi_only`、`cellular_only` | 使用英文小写加下划线 |
| 取值名称 | 面向用户展示的取值名称 | 仅 WiFi、仅 4G、WiFi+4G | 名称应稳定，避免随研发协议变化 |
| 协议值 | 设备协议、固件或接口中使用的实际值 | `1`、`2`、`3` | 如果存在多个协议版本，应进入协议映射表 |
| 取值说明 | 对该取值的业务解释 | 设备仅支持 WiFi 网络接入 | 不写版本信息 |
| 是否默认 | 是否为默认取值 | 是/否 | 一个功能项同一适用范围内只能有一个默认值 |
| 适用条件 | 该取值适用的机型或硬件条件 | 仅适用于带 4G 模组机型 | 应结构化关联条件 |
| 是否可售卖 | 是否可作为销售能力承诺 | 是/否 | 内部技术值不应对外暴露 |
| 排序 | 前端展示顺序 | 10、20、30 | 不得用排序值表达协议值 |
| 状态 | 取值状态 | 启用、废弃、停用 | 废弃后历史版本仍保留 |

关键规范：

- `协议值` 不是 `版本号`。
- `排序` 不是 `协议值`。
- `取值名称` 不是 `功能项名称`。
- `取值说明` 不能承载版本变更历史。
- 新增取值不一定等于功能集版本升级，只有当该取值进入某个已发布功能集版本时，才形成能力承诺变化。

### 11.5 功能集版本字段规范

功能集版本用于定义“某个能力包在某次发布中承诺支持什么”。它不是某个枚举数字，也不是某个固件版本。

建议字段如下：

| 字段 | 定义 | 示例 | 填写规则 |
| --- | --- | --- | --- |
| 功能集编码 | 功能集唯一标识 | `ipc_standard_feature_set` | 创建后原则上不可修改 |
| 功能集名称 | 功能集中文名称 | IPC 标准功能集 | 面向产品和业务展示 |
| 功能集版本号 | 产品侧能力版本号 | `V1.0.0`、`V1.1.0` | 使用语义化版本，不与协议枚举值混用 |
| 版本名称 | 便于用户理解的版本标题 | 2026 Q2 标准能力版 | 可选 |
| 版本说明 | 本版本整体说明 | 新增 4G 基站上报能力 | 描述变化，不列出所有字段 |
| 版本状态 | 当前版本状态 | 草稿、待评审、已冻结、已发布、已废弃 | 状态流转需受权限控制 |
| 生效时间 | 版本开始对业务可见或可引用的时间 | 2026-05-30 | 已发布版本必填 |
| 适用范围 | 适用产品线、区域、渠道或项目 | 国内 IPC 标准机型 | 结构化选择 |
| 变更类型 | 本版本变化性质 | 新增能力、取值扩展、兼容性调整、废弃能力 | 用于影响分析 |
| 关联功能项 | 本版本包含的功能项清单 | 网络连接类型、镜头数量 | 以版本快照方式保存 |
| 关联软件版本 | 支撑该能力版本的实现版本 | 固件 `FW_1.2.0`，App `8.5.0` | 可多选 |
| 验收状态 | 测试验证结论 | 未测试、测试中、通过、阻塞 | 未通过不得发布为对外承诺 |
| 审批记录 | 版本评审和发布审批记录 | 产品、研发、测试审批 | 留痕 |

版本号建议规则：

- `V1.0.0`：首个正式可发布能力基线。
- `V1.1.0`：新增向后兼容能力或新增可选取值。
- `V1.1.1`：不改变能力承诺的说明修订或小范围修正。
- `V2.0.0`：能力边界、适用范围或兼容性发生重大变化。

### 11.6 机型功能配置字段规范

机型功能配置用于定义“具体机型实际支持什么”。这是用户最关心的视图，也是产品规划必须补齐的核心对象。

建议字段如下：

| 字段 | 定义 | 示例 | 填写规则 |
| --- | --- | --- | --- |
| 机型编码 | 机型唯一标识 | `IPC-A100` | 与机型主数据一致 |
| SKU 编码 | 销售或生产 SKU | `IPC-A100-4G-CN` | 可选，但建议支持 |
| 硬件平台 | 设备硬件平台 | `平台 A` | 关联硬件主数据 |
| 硬件版本 | 硬件版本 | `HW V1.2` | 用于判断功能上限 |
| 标准功能集版本 | 该机型默认绑定的能力基线 | IPC 标准功能集 `V1.1.0` | 必填 |
| 项目功能集版本 | 项目定制能力基线 | 客户 A 定制功能集 `V1.0.0` | 可选 |
| 功能项取值 | 该机型实际启用的功能取值 | 网络连接类型=WiFi+4G | 必须来自功能取值表 |
| 支持状态 | 该功能对该机型的支持状态 | 支持、不支持、可选、License、预留 | 不能只用 0/1 表达复杂业务状态 |
| 生效批次 | 从哪个生产批次或版本开始生效 | 2026-06 批次 | 涉及硬件差异时必填 |
| 最低软件版本 | 使用该能力所需最低软件版本 | 固件 `FW_1.2.0` | 支撑售后和升级判断 |
| 对外承诺状态 | 是否可对客户承诺 | 可承诺、内部支持、暂不可承诺 | 销售视图只展示可承诺能力 |

### 11.7 字段命名规范

建议采用双层命名：产品侧中文名称 + 系统侧英文编码。

中文名称规范：

- 使用业务名词，不使用研发缩写。
- 使用“名词 + 类型/能力/支持/数量/模式”等稳定结构。
- 避免“是否支持某某”作为功能项名称，布尔型功能可以使用“某某支持”。
- 避免同义词混用，例如“网络类型”和“连接类型”必须界定差异。

英文编码规范：

- 使用小写英文加下划线。
- 不使用拼音。
- 不使用含糊缩写。
- 不把取值写进字段名。
- 不把版本号写进字段名。

示例：

| 不建议 | 建议 | 原因 |
| --- | --- | --- |
| `networkType` | `network_access_type` | 明确是网络接入类型，避免与协议连接类型混淆 |
| `connectType` | `device_protocol_type` | 如果表达 MQTT/TCP/NVR 等，应命名为设备协议类型 |
| `add_code` | `device_pairing_method` | “添加方式”应明确是配网/配对/绑定方式 |
| `haveMotor` | `pt_motor_type` | 表达云台电机类型，而不是是否有电机 |
| `supportsLowPower` | `low_power_wakeup_support` | 表达低功耗唤醒能力 |

### 11.8 功能分类标准

建议建立统一功能分类，避免所有字段堆在一个列表中。

初步分类建议：

- 设备基础信息：机型、SKU、硬件平台、传感器、镜头、外设。
- 网络连接能力：WiFi、4G、有线、蓝牙、蜂窝定位、弱网策略。
- 协议接入能力：IPC MQTT、NVR MQTT、TCP、ONVIF、GB/T 28181。
- 视频图像能力：分辨率、码流、帧率、WDR、夜视、补光、分屏。
- 音频能力：拾音、对讲、降噪、音频编码。
- 云台与电机能力：水平电机、垂直电机、预置位、巡航、追踪。
- 智能算法能力：人形、车辆、宠物、区域入侵、越界、人数统计。
- 低功耗能力：休眠、唤醒、PIR、电池、功耗模式。
- 存储能力：本地卡、NVR、云存储、循环录像。
- 云服务能力：基础云子码流、云告警、云录像、远程诊断。
- 运维诊断能力：日志、心跳、基站上报、远程配置、OTA。
- 商业控制能力：License、订阅、区域限制、渠道限制。

### 11.9 标准示例：网络连接类型

以截图中的“网络类型”为例，建议产品侧这样定义。

功能项：

| 字段 | 值 |
| --- | --- |
| 功能项编码 | `network_access_type` |
| 功能项名称 | 网络接入类型 |
| 功能分类 | 网络连接能力 |
| 功能定义 | 描述设备支持的网络接入方式，用于区分 WiFi、蜂窝网络、有线网络及其组合能力。 |
| 数据类型 | 枚举或多选枚举 |
| 是否业务可见 | 是 |
| 是否对外可承诺 | 是 |

功能取值：

| 取值编码 | 取值名称 | 协议值 | 取值说明 |
| --- | --- | --- | --- |
| `wifi_only` | 仅 WiFi | `1` | 设备仅支持 WiFi 接入。 |
| `cellular_only` | 仅 4G | `2` | 设备仅支持蜂窝网络接入。 |
| `wifi_cellular` | WiFi+4G | `3` | 设备同时支持 WiFi 和 4G 接入。 |
| `wired_cellular` | 有线+4G | `4` | 设备同时支持以太网和 4G 接入。 |
| `wired_wifi` | 有线+WiFi | `5` | 设备同时支持以太网和 WiFi 接入。 |
| `wired_only` | 仅有线 | `6` | 设备仅支持以太网接入。 |

注意事项：

- `1` 到 `6` 是协议值，不是功能集版本号。
- 若未来新增 `wired_wifi_cellular`，应新增一个取值，并评估是否进入某个功能集版本。
- 如果某机型支持 `wifi_cellular`，应在机型功能配置中表达，而不是修改功能项定义。

### 11.10 标准示例：设备协议类型

截图中“连接类型”包含 IPC MQTT、车载 TCP、BK TCP、NVR MQTT、宠物 MQTT、新 TCP、老 TCP 等，更像设备协议或接入链路类型，不应与“网络接入类型”混淆。

建议定义如下：

| 字段 | 值 |
| --- | --- |
| 功能项编码 | `device_protocol_type` |
| 功能项名称 | 设备协议类型 |
| 功能分类 | 协议接入能力 |
| 功能定义 | 描述设备与平台或网关之间采用的通信协议或接入链路类型。 |
| 数据类型 | 枚举 |
| 是否业务可见 | 内部可见或售前可见 |

产品判断：

- “网络接入类型”回答设备通过什么网络接入，例如 WiFi、4G、有线。
- “设备协议类型”回答设备用什么协议与平台通信，例如 MQTT、TCP、NVR MQTT。
- 这两个字段可以有关联，但不能合并。

### 11.11 标准示例：镜头数量

截图中的“镜头数量 0:无摄像头 1:一目 2:双目 3:三目”建议重新定义为结构化数值或枚举。

建议定义如下：

| 字段 | 值 |
| --- | --- |
| 功能项编码 | `camera_lens_count` |
| 功能项名称 | 镜头数量 |
| 功能分类 | 设备基础信息 |
| 功能定义 | 描述设备搭载的图像采集镜头数量。 |
| 数据类型 | 整数或枚举 |
| 单位 | 个 |

产品判断：

- 如果只是数量，建议用整数型。
- 如果“一目、双目、三目”背后代表不同产品形态和算法能力，建议用枚举，并补充能力含义。
- “无摄像头”对摄像机设备来说需要谨慎，可能应作为“不适用”或特殊设备类型，而不是普通取值。

### 11.12 标准示例：低功耗唤醒支持

截图中的“低功耗唤醒支持 0:不支持 1:支持”属于布尔能力，但产品定义不能只停留在是否支持。

建议定义如下：

| 字段 | 值 |
| --- | --- |
| 功能项编码 | `low_power_wakeup_support` |
| 功能项名称 | 低功耗唤醒支持 |
| 功能分类 | 低功耗能力 |
| 功能定义 | 描述设备是否支持在低功耗休眠状态下通过指定触发源唤醒并恢复工作。 |
| 数据类型 | 布尔 |
| 依赖条件 | 电池供电、低功耗芯片方案、PIR 或其他唤醒源 |

建议额外拆分的关联字段：

- 低功耗模式类型。
- 唤醒源类型。
- 唤醒时延。
- 唤醒后可用能力。
- 休眠期间是否支持远程预览。

原因是用户真正关心的不只是“支持/不支持”，而是支持到什么程度、在哪些场景可用、对体验有什么限制。

### 11.13 用户视角下的页面设计逻辑

平台不应把研发字段直接暴露给所有用户。建议按用户任务重构页面。

产品/研发维护视图：

- 展示功能项编码、功能项名称、分类、数据类型、取值数量、状态、维护人、最近变更时间。
- 点击进入详情后维护定义、取值、依赖、引用关系和版本历史。

业务查询视图：

- 默认按机型查询能力。
- 展示机型、功能分类、功能名称、支持状态、具体取值、最低软件版本、是否可对外承诺。
- 不展示协议值、内部编码、数据库 ID。

测试验收视图：

- 按功能集版本展示测试范围。
- 展示新增、变更、废弃能力。
- 展示关联测试用例、测试状态、缺陷和验收结论。

交付/售后视图：

- 按项目、机型、设备批次查询能力。
- 展示当前设备是否支持、是否需要升级、最低版本要求、替代方案。

### 11.14 从当前研发定义迁移到标准定义的步骤

建议采用渐进式治理，不建议一次性推翻重做。

1. 盘点现有功能项，识别功能项名称、取值、协议值、版本号混用情况。
2. 建立标准功能分类和命名规范。
3. 将“说明”字段中的枚举拆解为功能取值表。
4. 标记每个数字的真实含义：协议值、排序值、版本号、状态值或历史遗留值。
5. 对容易混淆的字段重命名，例如区分网络接入类型和设备协议类型。
6. 建立功能项引用关系，判断哪些字段已被机型、功能集或代码使用。
7. 先冻结旧字段，不直接删除。
8. 新建标准字段，并建立旧字段到新字段的映射。
9. 选择 1 到 2 个典型机型试点，完成标准功能集版本配置。
10. 通过试点验证查询、导出、测试和交付场景是否闭环。
11. 再批量迁移剩余机型和历史版本。

### 11.15 产品评审时必须回答的问题

每新增或修改一个功能项，产品评审必须回答：

- 这个字段描述的是功能项、功能取值、版本，还是机型配置。
- 这个字段的主要用户是谁。
- 用户看到这个字段后能否理解其业务含义。
- 这个字段是否需要对销售或客户可见。
- 这个字段是否影响硬件、固件、App、云平台、算法或测试。
- 这个字段是否有依赖或互斥关系。
- 这个字段的取值是否已经结构化。
- 这个字段是否会影响已发布功能集版本。
- 这个字段变更后哪些机型、项目、测试用例和文档需要同步。

## 12. 版本规划建议

### 12.1 MVP 阶段：先把能力管清楚

目标：建立功能项和功能集的基础数据规范，解决“功能定义分散、口径不一致”的问题。

建议范围：

- 功能项结构化管理。
- 枚举值结构化管理。
- 功能集创建和维护。
- 功能集版本草稿、发布、废弃。
- 机型与功能集版本绑定。
- 基础导入导出。
- 基础差异对比。
- 操作审计日志。

不建议 MVP 阶段过度建设复杂审批流，可以先用轻量状态机承载。

### 12.2 V1.1 阶段：把研发和测试闭环接上

目标：让功能集版本从“产品定义”进入“研发实现和测试验证”闭环。

建议范围：

- 功能集版本与固件、App、云平台、算法版本映射。
- 功能项与测试用例关联。
- 功能集版本生成测试范围。
- 测试结果回写。
- 发布准入检查。
- 版本冻结后变更控制。

### 12.3 V1.2 阶段：把业务查询和交付能力做强

目标：让平台从内部管理工具变成跨部门事实源。

建议范围：

- 按机型查询能力。
- 按项目查询能力。
- 多机型能力对比。
- 销售版能力清单导出。
- 交付版功能矩阵导出。
- 客服排障查询。
- 区域、渠道、License 差异管理。

### 12.4 V2.0 阶段：智能化和自动化

目标：基于沉淀的数据进行影响分析、风险预警和自动化推荐。

建议范围：

- 变更影响自动分析。
- 无效功能组合校验。
- 兼容性风险预警。
- 根据机型硬件能力推荐可选功能。
- 根据功能集版本自动生成测试建议。
- 与 PLM、Jira、Git、CI/CD、测试平台、设备管理平台集成。

## 13. 关键页面与能力建议

### 13.1 功能项详情页

建议从“编辑表单”升级为“功能项档案”：

- 基本信息。
- 枚举值或取值规则。
- 依赖与互斥。
- 适用范围。
- 被引用情况。
- 历史变更。
- 关联测试用例。
- 关联研发实现。

### 13.2 功能集版本详情页

建议突出版本基线：

- 版本号。
- 版本状态。
- 版本说明。
- 功能项清单。
- 与上一版本差异。
- 影响机型。
- 关联软件版本。
- 测试状态。
- 审批记录。
- 发布记录。

### 13.3 机型能力视图

建议面向业务人员设计：

- 机型基础信息。
- 当前标准功能集版本。
- 可售能力。
- 可选能力。
- 不支持能力。
- 区域差异。
- 项目差异。
- 推荐软件版本。
- 能力清单导出。

### 13.4 差异对比视图

建议支持：

- 功能集版本对比。
- 机型对比。
- 项目版本对比。
- 软件版本支持差异对比。

对比结果应标识：

- 新增。
- 删除或废弃。
- 取值变化。
- 状态变化。
- 依赖变化。
- 影响范围。

## 14. 权限与治理建议

建议按角色设置权限：

- 产品管理员：功能项、功能集、版本发布管理。
- 研发管理员：技术字段、软件版本映射、依赖关系维护。
- 测试管理员：测试用例和测试结果维护。
- 业务查看者：查询和导出已发布能力。
- 审批人：版本冻结、发布、废弃审批。
- 系统管理员：用户、角色、基础配置、审计。

关键治理原则：

- 草稿可编辑，冻结后不可直接编辑。
- 已发布版本不可覆盖修改，只能生成新版本。
- 已被引用的功能项不可物理删除。
- 项目定制不得默认进入标准功能集。
- 对外可见内容必须来自已发布版本。
- 所有关键变更必须留痕。

## 15. 指标建议

平台是否有效，不只看数据录入量，还应看是否减少跨部门沟通成本和交付风险。

建议指标：

- 功能项重复率下降。
- 机型能力查询耗时下降。
- 销售超卖或误卖问题数量下降。
- 因功能口径不一致导致的返工次数下降。
- 功能集版本发布准时率。
- 已发布功能集测试覆盖率。
- 已发布版本关联软件版本完整率。
- 历史版本追溯成功率。
- 项目定制转标准功能比例。

## 16. 主要风险与应对

### 16.1 数据治理风险

风险：历史功能项命名不统一、字段重复、枚举混乱。

建议：先建立功能项命名规范、编码规范、分类规范和去重机制，再批量迁移历史数据。

### 16.2 流程过重风险

风险：如果一开始就引入复杂审批，研发和产品可能绕开平台继续用线下表格。

建议：MVP 阶段使用轻量流程，先保证数据价值，再逐步增强审批和准入。

### 16.3 研发实现与产品定义脱节

风险：平台记录“支持”，但实际固件、App 或云平台版本未完全支持。

建议：功能集版本必须关联软件实现版本和测试验收状态，未验收不得发布为对外可承诺版本。

### 16.4 项目定制污染标准版本

风险：客户项目中的临时能力被直接写入标准功能集，导致标准版本越来越混乱。

建议：区分标准功能集、项目定制功能集和预研功能集，项目能力进入标准版必须经过产品评审。

### 16.5 历史追溯缺失

风险：功能项被直接编辑覆盖，导致无法解释历史机型当时支持什么。

建议：发布版本采用快照机制，历史版本不可变。

## 17. 当前平台优先优化建议

结合截图，建议优先做以下优化：

1. 将“说明”中的枚举值拆成结构化取值表。
2. 增加功能分类，例如网络连接、视频能力、音频能力、云服务、智能算法、低功耗、硬件配置、协议接入。
3. 增加功能项详情页，展示引用关系、版本历史和依赖互斥。
4. 建立功能集与功能集版本实体，而不只是单功能项版本。
5. 增加机型与功能集版本绑定关系。
6. 将“删除”改为“废弃/归档”，并增加引用校验。
7. 增加版本状态机：草稿、待评审、已冻结、已发布、已废弃。
8. 增加差异对比能力，优先支持版本对比和机型对比。
9. 增加导出能力，分别面向产品、测试、销售和交付。
10. 增加审计日志，记录关键变更人、时间、原因和影响范围。

## 18. 待进一步确认的问题

为了继续形成正式 PRD 或产品规划方案，建议后续确认：

- 当前平台是否已有机型、SKU、硬件版本数据。
- 功能项版本管理当前具体管理的是什么，是字段定义版本、枚举版本，还是功能支持版本。
- 功能集与机型之间是否已有绑定关系。
- 是否需要支持项目定制功能集。
- 是否已有固件、App、云平台、算法版本库。
- 是否需要与现有 PLM、PMS、Jira、测试平台或设备管理平台集成。
- 平台主要用户是研发内部，还是计划开放给产品、测试、销售、交付共同使用。
- 当前最痛的问题是数据分散、版本不可追溯、测试难对齐、销售难查询，还是项目定制混乱。

## 19. 初步结论

现有平台已经完成了从“无结构管理”到“功能项字典管理”的第一步，这是非常有价值的基础。但从专业产品管理角度看，它目前更像研发字段管理工具，还未完全成为机型功能集版本治理平台。

后续规划的重点不应只是继续增加字段，而应围绕“能力定义、版本基线、机型绑定、实现映射、测试验证、发布可见、历史追溯”建立闭环。

建议先以 MVP 方式补齐功能项结构化、功能集版本基线、机型绑定、状态机和审计能力，再逐步扩展到研发测试闭环、业务查询闭环和智能影响分析。这样既能保留研发团队已有成果，又能把平台升级为真正支撑摄像机 IoT 产品规划、交付和生命周期管理的核心系统。
